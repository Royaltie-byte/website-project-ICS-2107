const form=document.getElementById('reservation').addEventListener('submit', function (event) {
    event.preventDefault(); alert('Your Information Has Been Received Successfully!');
});

const form2=document.getElementById('reservation').addEventListener('submit', function (event) {
    event.preventDefault(); alert('You will receive a confirmation message shortly!');
});


